# BCI Motor Imagery AutoML Framework
# Python 3.10+

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
PyYAML>=6.0
matplotlib>=3.7

# EEG / BCI dataset (GDF)
mne>=1.6

# Optional: wavelet preprocessing/features
PyWavelets>=1.4

# Optional: PyTorch for EEGNet
torch>=2.0
# torch is optional; LDA/SVM/RF work without it

# Optional: Riemannian (install pyriemann for better covariance features)
# pyriemann>=0.5

# Optional: MOABB for extra datasets and cross-dataset benchmarking (pip install moabb)
# moabb>=1.1

# Web interface (--web): detailed zoomable EEG view
fastapi>=0.109
uvicorn[standard]>=0.27
websockets>=12.0
