# Examples

See the main **README.md** in the project root for full overview, usage, and scripts.

- `run_offline.py`, `run_online.py` — programmatic runs.
- `bci_automl_minimal_example.ipynb` — notebook: load data, calibration, leaderboard, snapshots. Run from project root with `PYTHONPATH=.` or from `examples/` with `PYTHONPATH=..`.
